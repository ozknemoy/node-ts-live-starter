<!DOCTYPE html>
<html ng-app="docx-upload">
<head>
  {{> v2_imports_css}}
  {{> common_html_head}}
  {{> common_imports}}
  {{> angular_imports}}
  <script>
    function payTest(fileName) {
      var xhr = new XMLHttpRequest();
      xhr.open('POST', '/file-parse/pay-for-file-from-acquiring/' + fileName, false);
      xhr.send();
    }
  </script>
</head>
<body>

<section class="page">
  <div class="navbar-static-top">
    <div class="container">
      <ul class="nav brand">
        <li><a href="/"><span>{{$ctrl.GLOBALS.siteName}}</span></a></li>
      </ul>
      <ul class="nav menu">

      </ul>
    </div>
  </div>

  <div class="container">
    <div class="row intro">
      <div class="col-md-offset-3 col-md-9">
        <h1 class="h1AsH3">Обход антиплагиата документа онлайн</h1>
        <p>
          {{#if $ctrl.filePayInfo.payed}}
            <h3>Ваш документ {{ $ctrl.fileName}} оплачен. Письмо с файлом отправлено на указанный вами почтовый ящик</h3>
            <a href="{{$ctrl.afterPayUrl}}">Здесь</a> вы можете повторно сделать обработку, изменив степень уникальности.
          {{else}}
            <h3>Ваш документ {{ $ctrl.fileName}} успешно обработан.</h3>
            <button onclick="payTest('{{$ctrl.fileName}}')">Тестовая оплата</button>
            <br>
            <small>после нажатия кнопки на почту прилетит письмо об успешной оплате</small>
          {{/if}}
        </p>
      </div>
    </div>
  </div>

  <div class="shade">
    <div class="container">
      <div class="row">
        <div class="col-md-6 col-md-offset-6">
          <h3>Вы можете оплатить следующими способами</h3>
          <br><br><br><br><br><br><br><br>
        </div>
      </div>
    </div>
  </div>
</section>

<h3 class="sticky left">ПОВЫШЕНИЕ УНИКАЛЬНОСТИ</h3>
<h3 class="sticky right">{{$ctrl.GLOBALS.siteName}}</h3>

{{> footer}}
</body>
</html>